# Tala Systems Clean URL Migration

Public pages now use directory-style URLs:
- `/leadership/`
- `/join/`
- `/collaborate/`
- `/projects/cdms/`

The original `.html` URLs remain as lightweight redirect stubs so existing bookmarks and indexed links can transition safely.

Internal navigation, canonical URLs, and `sitemap.xml` point to the clean URLs.
